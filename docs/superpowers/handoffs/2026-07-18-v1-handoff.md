# xwobart — handoff: continue from v0 (tasks in order: A → B → C)

Date: 2026-07-18
For: a fresh conversation picking up after v0 is complete.
Recommended sequence: **A (quick uncertainty check) → B (v1 spray + handedness) → C (PSIS-LOO)**.

You're picking up **xwobart**, a Bayesian xwOBA rebuild, at
`/Users/jweinga/Documents/python/xwobart` (git remote origin = github.com/bwista/xwobart,
branch `main`). v0 is COMPLETE and pushed. Do the three tasks below in order.

## First, ground yourself (read these)
- `results/RESULTS.md` — v0 outcome, the Stage C decision, and the full deviations list.
- `docs/superpowers/specs/2026-07-17-xwobart-v0-design.md` — the design authority (see §2
  "non-goals / later rungs": v1 = spray angle + handedness).
- `src/{config,data,prep,model,rollup,evaluate}.py` and `scripts/run_v0.py` — the pipeline.
- Your memory note "xwobart v0 status" if surfaced.

## Environment + hard-won gotchas (don't re-derive these)
- venv: `.venv` (Python 3.12 framework build). Run everything from the repo root.
  `.venv/bin/pytest` (28 tests, no MCMC); `.venv/bin/python scripts/run_v0.py --stage A|B|C`.
- Resolved deps are NEWER than the original plan assumed: pymc 6.1, pymc-bart 0.12,
  arviz 1.2, polars 1.42, pandas 3.0, numpy 2.4. Exact pins in `requirements.lock`.
- Data: `STATCAST_PATH` (`.env`) points at the local monthly Statcast parquet cache; the repo
  depends on `-e ../kinferencetoolkit` for `pipeline.statcast_loader` + `pipeline.player_names`.
  The only network pulls are the sprint-speed and expected-stats leaderboards (cached).
- pymc-bart 0.12: the documented `pm.set_data` + `sample_posterior_predictive` OOS path
  SILENTLY FREEZES `mu` (returns the in-sample trace regardless of X_new). The live path is
  `src.model.predict_mu_from_trees` (stored trees via `_sample_posterior`, which returns
  (S,n,K) → transpose to (S,K,n)). Keep using `predict_mu_any`.
- Any script that FITS must put executable code under `if __name__ == "__main__":`
  (pymc-bart's Manager + pymc parallel chains use spawn and re-import the module).
- arviz 1.2 ships no NetCDF backend → `h5netcdf` + `h5py` are required (already in requirements).
- BART `mu` R-hat is STRUCTURALLY high (~1.2–1.8), not a convergence signal. Gate real
  convergence on `verify_oos_mechanism` (corr ~1.0). `run_v0` already treats R-hat as a warning.
- The 3-feature model saturates by ~50k training rows (Stage C @100k ≈ Stage B @50k).
  MCMC is non-deterministic (spawn RNG) — expect small run-to-run variation; don't chase it.

## The v0 anchor to beat
ELPD (lppd) = **−80107 ± 244** on the 2025 holdout (122k events). Also: player-season
r 0.96 (100+ PA), event-level r 0.91, weighted ECE 0.042. Artifacts in `results/stage_{A,B,C}/`
(`idata.nc` is gitignored; `metrics.json`, `figures/`, `player_table.parquet` are tracked).

---

## TASK A — uncertainty sanity-check + disagreement leaderboard (quick, NO re-fit)
Purpose: validate v0's core value-add (per-player credible intervals) before investing in v1.
Use `results/stage_C/player_table.parquet` (cols: batter, player_name, season, PA, xwoba_mean,
xwoba_sd, xwoba_q05, xwoba_q95, xwoba_savant).
1. Confirm interval width behaves: regress `log(xwoba_q95 − xwoba_q05)` on `log(PA)` for 100+ PA
   players — expect slope ≈ −0.5 (uncertainty ~1/√PA). Plot width vs PA.
2. Surface the largest `|xwoba_mean − xwoba_savant|` players (100+ PA) — where the model most
   disagrees with Savant; sanity-check a few by hand (fast/slow players, extreme BBE mixes).
Deliver a short script + 1–2 figures + a markdown note. No MCMC. Fine to do in the same session.

---

## TASK B — v1 = spray angle + handedness (the real advance)
Why: speed's payoff concentrates on pull-side/infield hits, exactly what v0 (EV/LA/sprint only)
can't see — which is why v0's speed-undercorrection test (§9.4) was inconclusive.

Scope:
- Data (`src/data.py`): add `hc_x`, `hc_y`, `stand` to `KEEP_COLUMNS`; rebuild slim caches with
  `--force-data`. Derive spray angle from hit coords via the standard Statcast formula
  (~ `atan2(hc_x − 125.42, 198.27 − hc_y)` in degrees, optionally signed by handedness for
  pull/oppo). VERIFY the constants/sign against the cached data (e.g. pulled HRs land on the
  handedness-expected side) — don't trust the formula blind. Handle missing hit coords like
  the existing LS/LA drop.
- Features (`src/prep.py`): extend `build_features` from 3 → 5: launch_speed, launch_angle,
  spray_angle, sprint_speed, and handedness (0/1). Keep the BART 5-class categorical structure.
- Re-run the A/B/C ladder via `run_v0`. Success = beat ELPD −80107, and show the localization /
  undercorrection signals sharpen (pull-side + speed interaction should now be visible).
- Do NOT add bat tracking (`bat_speed`/`swing_length`) here — that's v2 and only exists 2024+,
  which would shrink training to 2 seasons. Spray + handedness exist for all seasons.

Process: run this as its own spec→plan→execute cycle like v0 — use the superpowers
brainstorming, then writing-plans, then executing-plans (TDD; unit tests for pure logic; MCMC
only in the smoke + stages). Honor the stop-and-report gates: after Stage A, after Stage B's four
checks, and for the Stage C full-vs-100k decision. Work directly on `main` (the repo convention —
no worktree). Commit per task; push only when asked.

---

## TASK C — principled model comparison (hygiene)
Swap the raw-ELPD anchor for arviz PSIS-LOO so v0→v1→v2 carry SEs and compare apples-to-apples
(`az.loo` / `az.compare`). Note: pymc-bart may not write pointwise log-likelihood into idata
automatically — you likely need to assemble it from the per-event log scores already computed in
`model.predict_and_reduce` (`lppd_i` / `meanlog_i`) or add a `log_likelihood` group. Record the
LOO numbers for v0 and v1 in RESULTS.md.

---

## Definition of done (v1, mirrors v0 §15)
v1 beats the ELPD anchor (or a clear reason why not); all four acceptance checks recomputed with
plausible values; `results/RESULTS.md` updated (results + deviations + the model-comparison
table); all unit tests green.
